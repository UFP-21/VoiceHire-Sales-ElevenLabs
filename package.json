{
  "name": "voicehire-sales-elevenlabs",
  "version": "0.1.0",
  "private": true,
  "description": "VoiceHire Sales AI demo with ElevenLabs ElevenAgents Realtime.",
  "type": "module",
  "workspaces": [
    "backend",
    "frontend",
    "shared"
  ],
  "scripts": {
    "dev": "npm run dev --workspace backend",
    "dev:frontend": "npm run dev --workspace frontend",
    "dev:backend": "npm run dev --workspace backend",
    "build": "npm run build --workspace shared && npm run build --workspace frontend && npm run build --workspace backend && node scripts/copy-frontend.mjs",
    "typecheck": "npm run build --workspace shared && npm run typecheck --workspace frontend && npm run typecheck --workspace backend",
    "test": "npm run test --workspace shared && npm run test --workspace backend && npm run test --workspace frontend",
    "lint": "npm run lint --workspaces --if-present"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@eslint/js": "^9.32.0",
    "eslint": "^9.32.0",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.38.0"
  }
}
